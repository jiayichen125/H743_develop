这是一个关于**“基于时域统计特征与概率分布的波形识别方案”**的详细技术文档。该方案避开了传统的 FFT 运算，转而利用信号在时域的几何特性和统计分布规律。

---

# 高频信号（100Hz - 100kHz）时域判别方案技术文档

## 1. 方案原理分析

本方案的核心思想是：**不同波形的电压值在时间轴上的分布（概率密度函数 PDF）具有显著的几何特征差异。**

### 1.1 统计分布特性
* **方波 (Square Wave)：** 电压绝大多数时间处于 $V_{max}$ 或 $V_{min}$，其 PDF 呈现极端的“双峰分布”。除了跳变沿，其导数 $dy/dt$ 几乎处处为 0。
* **正弦波 (Sine Wave)：** 电压在波峰和波谷处变化最慢（停留时间长），在中点处变化最快。其 PDF 呈现“U型分布”。
* **三角波 (Triangle Wave)：** 电压随时间线性变化，各电压值出现的概率相等。其 PDF 呈现“均匀分布”。



### 1.2 关键判别特征量
1.  **波形因子 ($K_f$):** $$K_f = \frac{V_{rms}}{V_{avg}}$$。方波为 $1.0$，正弦波为 $1.11$，三角波为 $1.15$。
2.  **峰值占比 ($R_{peak}$):** 信号落在峰值区间（如 $V_{pp}$ 的上下 $10\%$ 范围内）的点数与总点数的比例。
3.  **平坦度/稀疏度 ($S$):** 计算一阶差分 $\Delta y = |y_n - y_{n-1}|$，统计 $\Delta y < \text{Threshold}$ 的比例。

---

## 2. 极限情况验证

* **欠采样 (Under-sampling):** 当信号频率（$100\text{kHz}$）接近采样率时，单周期采样点极少。
    * *验证：* 只要采样时钟与信号不同步（存在相位漂移），累积多个周期的点，统计规律依然服从 PDF 规律。**结论：有效。**
* **低信噪比 (Low SNR):** 噪声会模糊方波的跳变沿，导致“平坦度”下降。
    * *验证：* 需要引入动态噪声门限 $\epsilon$。通过 $V_{pp}$ 比例设置 $\epsilon$ 可抵消部分影响。**结论：受限，但优于 FFT 峰值检测。**
* **幅度极小:** ADC 精度量化误差增加。
    * *验证：* H7 的 $16\text{-bit}$ ADC 在 $V_{pp} > 100\text{mV}$ 时特征依然明显。**结论：有效。**

---

## 3. 算法核心代码 (C 语言)

针对 STM32H7 优化，利用 FPU 进行计算。

```c
#include <math.h>
#include <stdint.h>
#include <stdlib.h>

typedef enum { SINE, SQUARE, TRIANGLE, UNKNOWN } WaveType;

WaveType ClassifyWaveform(uint16_t* adc_buf, uint32_t len) {
    float sum_abs = 0, sum_sq = 0;
    uint32_t peak_count = 0;
    uint16_t max_v = 0, min_v = 4095;
    
    // 1. 获取基础极值
    for(uint32_t i=0; i<len; i++) {
        if(adc_buf[i] > max_v) max_v = adc_buf[i];
        if(adc_buf[i] < min_v) min_v = adc_buf[i];
    }
    
    float vpp = max_v - min_v;
    float offset = min_v + vpp/2.0f;
    float threshold = vpp * 0.10f; // 10% 容差

    // 2. 统计计算
    for(uint32_t i=0; i<len; i++) {
        float val = (float)adc_buf[i] - offset;
        sum_abs += fabsf(val);
        sum_sq += val * val;
        
        // 统计峰值区间占比
        if(adc_buf[i] >= (max_v - threshold) || adc_buf[i] <= (min_v + threshold)) {
            peak_count++;
        }
    }

    float v_rms = sqrtf(sum_sq / len);
    float v_avg = sum_abs / len;
    float k_f = v_rms / v_avg;      // 波形因子
    float r_peak = (float)peak_count / len; // 峰值占比

    // 3. 判别逻辑
    if (r_peak > 0.80f && k_f < 1.05f) return SQUARE;   // 方波：大部分点在两头，Kf接近1
    if (r_peak > 0.30f && r_peak < 0.50f && k_f < 1.13f) return SINE; // 正弦：峰值停留中等
    if (r_peak < 0.25f && k_f > 1.13f) return TRIANGLE; // 三角：峰值停留极短，Kf大

    return UNKNOWN;
}
```

---

## 4. 测试数据表 (20组预期观测值)

以下数据基于 $1024$ 点采集。

| 序号 | 信号类型 | 频率 | 波形因子 $K_f$ | 峰值占比 $R_{peak}$ | 预期结果 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | 方波 | 100Hz | 1.002 | 0.96 | SQUARE |
| 2 | 方波 | 1kHz | 1.005 | 0.94 | SQUARE |
| 3 | 方波 | 50kHz | 1.012 | 0.91 | SQUARE |
| 4 | 方波 | 100kHz | 1.025 | 0.88 | SQUARE |
| 5 | 方波 (高噪) | 10kHz | 1.040 | 0.82 | SQUARE |
| 6 | 正弦波 | 100Hz | 1.111 | 0.38 | SINE |
| 7 | 正弦波 | 5kHz | 1.112 | 0.37 | SINE |
| 8 | 正弦波 | 20kHz | 1.110 | 0.39 | SINE |
| 9 | 正弦波 | 80kHz | 1.115 | 0.35 | SINE |
| 10 | 正弦波 | 100kHz | 1.118 | 0.34 | SINE |
| 11 | 正弦波 (低幅) | 1kHz | 1.125 | 0.42 | SINE |
| 12 | 三角波 | 100Hz | 1.155 | 0.20 | TRIANGLE |
| 13 | 三角波 | 2kHz | 1.156 | 0.20 | TRIANGLE |
| 14 | 三角波 | 10kHz | 1.154 | 0.21 | TRIANGLE |
| 15 | 三角波 | 60kHz | 1.160 | 0.19 | TRIANGLE |
| 16 | 三角波 | 100kHz | 1.165 | 0.18 | TRIANGLE |
| 17 | 锯齿波 (类似) | 1kHz | 1.158 | 0.21 | TRIANGLE |
| 18 | 方波 (占空比30%) | 1kHz | 1.080 | 0.85 | SQUARE |
| 19 | 噪声信号 | - | 1.300+ | 0.10 | UNKNOWN |
| 20 | 直流信号 | - | 1.000 | 1.00 | UNKNOWN/DC |

---

## 5. 总结

本方案利用了信号的**二阶统计矩（有效值）**与**一阶概率分布（峰值占比）**的组合判别。相比 FFT，它对内存的需求极低（只需原始缓存），且在 H7 强大的 CPU 下，处理速度可以轻松跨越至 **MHz** 级别的特征提取。

## 6. Plan: 波形识别算法升级（统计法 + FFT 联合判断）

**核心思路**：用"波形因子 $K_f$ + 峰值占比 $R_{peak}$"双特征时域统计法替换现有 FFT 谐波比值法，低频段保留 FFT 辅助投票，高频段完全切换为统计法，解决谐波超奈奎斯特的根本问题。

---

**Steps**

### Phase 1：实现 ClassifyWaveform 统计函数
1. 在 fft.c 新增 `ClassifyWaveform()`，一次遍历 `ADC_Buffer` 计算：
   - `vpp = max_v - min_v`（注意 H743 是 16bit，`min_v` 初始化为 **65535** 而非文档的 4095）
   - `Kf = v_rms / v_avg`
   - `Rpeak`（峰值区间 = vpp 上下 10%）
   - 判别阈值按文档第3节：`Rpeak > 0.80 && Kf < 1.05` → 方波；`Rpeak < 0.25 && Kf > 1.13` → 三角波；否则 → 正弦波
2. 信号过弱保护：`vpp < 655`（≈0.033V）时返回 UNKNOWN，输出"未知"

### Phase 2：改造 wave_type_detect 为联合判断
3. **低频段**（`BaseIdx < 171`，基波 < 16.7kHz）：沿用 FFT 谐波比值法
4. **高频段**（`BaseIdx >= 205`，基波 > 20kHz）：直接使用 `ClassifyWaveform`
5. **过渡区**（171 ≤ BaseIdx < 205）：两法各出结论，一致则采用，不一致则**信任统计法**

### Phase 3：验证与调参
6. 用信号发生器测试 3种波形 × 5个频率点（100Hz / 1kHz / 10kHz / 50kHz / 100kHz）= 15组用例
7. 对照文档第4节20组预期值核查 $K_f$ 和 $R_{peak}$ 实测数值
8. 根据实测结果微调阈值（理论值与实际 ADC 量化误差有偏差）

---

**Relevant files**
- fft.c — 改造 `wave_type_detect()`，新增 `ClassifyWaveform()`
- fft.h — 若需外部调用则补充声明

---

**Verification**
1. 编译通过，无新增 warning
2. 100kHz 方波 → "方波"，22.3kHz 三角波 → "三角波"（当前两个失效用例必须修复）
3. 1kHz 正弦波 → "正弦波"（原有用例不退化）
4. 对照文档表格第4节逐行比对 $K_f$ 与 $R_{peak}$ 实测值

---

**Decisions**
- 过渡区冲突时信任统计法（FFT谐波在该区间已开始失真，统计法更可靠）
- 本次只动波形判断逻辑，**不改动**幅值/频率计算部分
- `ClassifyWaveform` 设为文件内 `static`，不需要在 `.h` 中声明